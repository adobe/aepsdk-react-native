# Legacy: `awesome-project/` notes

Error/fix documentation for **AwesomeProject** now lives under **`../errors/`**, one file per error type (see **`../README.md`**).

The previous single file `TROUBLESHOOTING.md` was split; the Metro + `@babel/runtime` entry is in:

`../errors/awesome-project-metro-babel-runtime-unresolved.md`
